def optimize_snowphase(override):
    override('Actors/Snow.cs', [('private Rng flakeRng = new(0);', 'private Rng flakeRng = new(0);\n    private readonly List<Vec3> frameFlakeOffsets = [];'), ('var billboardLeft = World.Camera.Left * 0.50f;', 'frameFlakeOffsets.Clear();\n        var billboardLeft = World.Camera.Left * 0.50f;'), ('if (flakeSeeds.Count <= i)', 'if (frameFlakeOffsets.Count <= i)\n                {\n                if (flakeSeeds.Count <= i)'), ('var seed = flakeSeeds[i];\n                Vec3 point = new()', 'var seed = flakeSeeds[i];\n                frameFlakeOffsets.Add(new Vec3(\n                    Mod(seed.X + seed.VX * time * Direction.X, area),\n                    Mod(seed.Y + seed.VY * time * Direction.Y, area),\n                    Mod(seed.Z + seed.VZ * time * Direction.Z, area)));\n                }\n                var offset = frameFlakeOffsets[i];\n                Vec3 point = new()'), ('x + Mod(seed.X + seed.VX * time * Direction.X, area)', 'x + offset.X'), ('y + Mod(seed.Y + seed.VY * time * Direction.Y, area)', 'y + offset.Y'), ('z + Mod(seed.Z + seed.VZ * time * Direction.Z, area)', 'z + offset.Z')])
